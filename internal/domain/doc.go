// Package domain holds tapeIt's core types and ports (interfaces). It has no
// dependencies on Spotify, Apple, HTTP, or storage — those are adapters that
// implement the ports defined here. See docs/DESIGN.md.
package domain
